locals {
  name_prefix = "${var.project_name}-${var.environment}"
}

# Resource definitions will be added in a later step.
# Planned resources include VPC, subnets, route tables, and internet/NAT gateways.
